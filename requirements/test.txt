Sphinx
sphinx_rtd_theme==0.3.0
flake8
mock==2.0.0
freezegun==0.3.7
xmldiff==1.1.1

hypothesis==3.6.0
unittest2==1.1.0
Faker==0.8.10

pytest==3.6.1
pytest-django==3.3.0             # Django specific fixtures, settings handling
pytest-sugar==0.9.1              # Adds a progressbar and shows tests failures as they happen
pytest-pythonpath==0.7.2         # Fix the project path no matter where tests are run
pytest-mock==1.10.0              # Wrapper around mock library

IPython==5.1.0
